server:
  port: 18099

dubbo:
  application:
    name: auth-service
  registry:
    address: nacos://40.81.229.5:8848
    username: nacos
    password: nacos
    # Or disable QoS
    qos-enable: false
  consumer:
    timeout: 5000  # 全局消费者超时时间（毫秒）

# GitHub OAuth配置
github:
  oauth:
    client-id: ${GITHUB_CLIENT_ID:Ov23liafMWANTJ7vZOiA}
    client-secret: ${GITHUB_CLIENT_SECRET:5dc76839cb44ab7a09f78334a9ccc770d0993cc3}
    redirect-uri: ${GITHUB_REDIRECT_URI:http://127.0.0.1:18099/auth/github/callback}

# WeChat OAuth配置
wechat:
  oauth:
    app-id: ${WECHAT_APP_ID:your_app_id}
    app-secret: ${WECHAT_APP_SECRET:your_app_secret}
    redirect-uri: ${WECHAT_REDIRECT_URI:http://127.0.0.1:18099/auth/wechat/callback}
logging:
  level:
    com.ncwu.authservice: debug